# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Config / validation
pydantic==2.9.2
pydantic-settings==2.5.2

# DB
sqlalchemy[asyncio]==2.0.34
asyncpg==0.29.0
alembic==1.13.2
greenlet==3.1.0  # required transitively by sqlalchemy[asyncio]; pin so wheels land

# Queue / cache
redis==5.0.8

# HTTP + LLM client
httpx==0.27.2
anthropic==0.45.0  # Used with custom base_url for Kimi + MiniMax (anthropic-messages protocol)

# Google Sign In (Phase 11.5) — validate Google-issued ID tokens
google-auth==2.34.0

# Misc
python-dateutil==2.9.0
langdetect==1.0.9  # bilingual detection (es/en) for inbound messages

# File import (Phase 12) — extract leads from uploaded files
pypdf==5.1.0            # PDF text extraction
openpyxl==3.1.5         # XLSX extraction
pillow==10.4.0          # image decoding for OCR
pytesseract==0.3.13     # OCR wrapper (needs system tesseract-ocr — see Dockerfile)

# Test (kept in primary requirements so the image is self-sufficient for `docker exec pytest`)
pytest==8.3.3
pytest-asyncio==0.24.0
